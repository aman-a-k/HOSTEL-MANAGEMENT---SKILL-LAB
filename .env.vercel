# Vercel Deployment Configuration
# This file is used by Vercel to build and deploy the frontend

# Do not add secrets here - set them in Vercel dashboard under Environment Variables

# Frontend API URL - set in Vercel dashboard
VITE_API_URL=
